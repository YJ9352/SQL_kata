// 1. 이름이 있는 동물의 아이디
SELECT ANIMAL_ID
FROM ANIMAL_INS 
WHERE NAME IS NOT NULL 

// 2. 역순 정렬하기
SELECT NAME, DATETIME
FROM ANIMAL_INS
ORDER BY ANIMAL_ID DESC

// 3. 중복 제거하기
SELECT COUNT(DISTINCT NAME) 
FROM ANIMAL_INS 
WHERE NAME IS NOT NULL
